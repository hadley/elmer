# as_json_schema specialised for OpenAI

    Code
      as_json_schema(stub, type_object(.additional_properties = TRUE))
    Condition
      Error:
      ! `.additional_properties` not supported for OpenAI.

